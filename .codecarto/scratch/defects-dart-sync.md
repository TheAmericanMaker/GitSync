# Dart sync orchestration defect findings

Generated 2026-05-06 by defect-scan subagent B. Files scanned: gitsync_service.dart, manager/{git_manager, storage, settings_manager, repo_manager, premium_manager}.dart, api/{helper, logger, accessibility_service_helper}.dart (~140KB total).

## Pass 1 — Logic and Correctness

| # | Location | Defect | Severity | Evidence Level | Action | Notes |
|---|---|---|---|---|---|---|
| D1.1 | `gitsync_service.dart:_sync` (early returns at remotes/credentials/conflicting/gitDirPath/innerError branches) | Calls `_displaySyncMessage(null, ...)` with `settingsManager == null`. The function tests `settingsManager == null \|\| await settingsManager.getBool(...)` so a `null` argument **always shows the message**, ignoring the user's `setman_syncMessageEnabled=false` preference. All five "credentials not found / merge conflict / repo not found" calls bypass the user toggle. | medium | observed | Pass `settingsManager` (already in scope) instead of `null`, or rename the parameter to make "force-show" intent explicit. | `_displaySyncMessage(SettingsManager? sm, ...) { if (sm == null \|\| await sm.getBool(...)) { ... show ...} }` — a `null` short-circuits the gate. |
| D1.2 | `gitsync_service.dart:_sync` lines ~226-233 (terminal disposition) | After the inner async closure, `if (pushResult == null \|\| pullResult == null)` logs `"Sync failed"` but does **not** set `terminal = 'error'`. The widget therefore renders 'success' on a tri-state failure when `innerError` was already cleared by an earlier branch. The `innerError` flag is set in the closure but if both `pullResult==null` happens via `case null: { innerError = true; return; }` it works — but if `pushResult == null` after a successful pull (case null returns from closure with `innerError = true`), the only assignment to `terminal` is via `if (innerError)`. So this works for the specific paths that hit `case null`, but `pullResult == null \|\| pushResult == null` outside that flow (e.g. recommendedAction short-circuit leaves both `false`, never evaluated as null) is fine. The gap: the "Sync failed" log branch does not set `terminal='error'`, relying on `innerError` instead. | medium | strong inference | Add `terminal = 'error';` inside the `if (pushResult == null \|\| pullResult == null)` branch, or remove the redundant log branch since `innerError` already handles it. | Defensive programming gap; current code is correct only because `innerError` is set from inside `case null` blocks. |
| D1.3 | `gitsync_service.dart:_sync` lines ~262/274 (`optimisedSyncFlag` short-circuit) | The second `if (optimisedSyncFlag && recommendedAction == -1) return;` (after re-querying `getRecommendedAction`) returns from the inner closure with `pushResult = false`, `pullResult = false`. The outer code then hits the `else` branch ("Sync Complete!"), shows `syncNotRequired` if forced, and reports success. **However, if the pull phase actually downloaded changes and set `pullResult = true`, then `recommendedAction == -1` re-checked *after* the pull will skip the push — which is correct. But the local `bool synced` variable is also lost.** More importantly, the second short-circuit happens **after** `getConflicting` re-check, so if conflicts arose the conflicts branch fires first (correct). The *real* bug: when the second short-circuit hits, `pullResult` may legitimately be `true` from a successful pull. Then the outer `pushResult == true \|\| pullResult == true` branch will still call `getRecentCommits()` and `syncComplete` — that's fine. So the gate is *correct in steady state*, but consumes CPU re-querying recommendedAction needlessly when the first call already returned -1 (the first guard would have returned). Mark as **low / dead code** — the second short-circuit can only fire when the first did NOT (i.e. recommendedAction transitioned to -1 *after* a pull), which is a legitimate observation. | low | observed | None required; document that the re-query is intentional for the post-pull state. | Verified the gate is correct, but the duplication is fragile — extract to a helper. |
| D1.4 | `gitsync_service.dart:_sync` line 291 `await GitManager.getRecentCommits(priority: 3);` | Called twice in success paths: once inside the `else if (pushResult == true \|\| pullResult == true)` branch (line 281) and again unconditionally as the last line of the `try` (line 291). The unconditional call also fires on the "no changes" success path, which is **wasteful but not incorrect**. On the credentials-missing / no-remote / repo-not-found early-returns, it does NOT fire (those `return` early). | low | observed | Remove the inner call or the outer unconditional call. | Performance/log-noise only. |
| D1.5 | `gitsync_service.dart:merge` (lines ~290-336) | Network-error early-return path calls `serviceInstance?.invoke(MERGE_COMPLETE)` and returns — good. But the `pushResult == false` ("Merge Not Required") path falls through to the final `serviceInstance?.invoke(MERGE_COMPLETE)` correctly. The `case true` "Merge Complete" path also falls through. **However, if `getClientModeEnabled()` is true, the `backgroundStageAndCommit` is called, but client mode then **skips** `debouncedSync(repomanRepoindex, true)` at line 333 — so a manual merge in client mode commits but never pushes the merge resolution.** This may be intentional (client mode = no remote sync) but is worth flagging because the UI completion signal fires before any push. | medium | strong inference | Verify client-mode merge semantics; add a comment if the no-push behavior is intentional. | Fold into semantic pass if needed. |
| D1.6 | `gitsync_service.dart:accessibilityEvent` lines 338-376 | `lastOpenPackageName` is updated unconditionally at line 374 but `lastOpenPackageNameExcludingInputs` is only updated when the package isn't an input method (line 376). The dispatch logic at lines 354/365 uses `lastOpenPackageNameExcludingInputs` for both close-detect and open-detect. **Edge case:** if the user opens IME→app A (excl=A)→IME→app B (excl=B since A was tracked), the close-detect compares `packageNames.contains(lastOpenPackageNameExcludingInputs=A) && !packageNames.contains(B)` — correct. But if A is an input method that *also* matches `packageNames`, A never enters `lastOpenPackageNameExcludingInputs`, and "close" of A is never detected. | low | strong inference | If `packageNames` may legitimately contain IME packages, separate the two semantics. | Edge case; not a typical user setup. |
| D1.7 | `gitsync_service.dart:accessibilityEvent` line 343 `await SettingsManager().reinit(repoIndex: index)` inside loop | Each iteration creates a fresh `SettingsManager()` and reinits it, but `keyNamespace` is a **static** field on the class. The static is mutated mid-loop, so any concurrent `accessibilityEvent` invocation (or anything else touching `SettingsManager.keyNamespace`) races on the static. This is the documented `arch-CF9` concurrency hazard but the *synchronous* issue is: within a single isolate, between the `await getApplicationPackages()` and the `await getBool` calls, a re-entrant `accessibilityEvent` (e.g. retrigger from a different thread of `MethodChannel` callbacks) would corrupt `keyNamespace`. Routed to semantic. | — | observed | Route to semantic. | See routed table. |
| D1.8 | `gitsync_service.dart:debouncedSync` line ~178 `debounce(repomanRepoindex.toString(), 500, () => _sync(...))` | The debounced callback is `_sync` which is `Future<void>` — the callback returned by `debounce` is `VoidCallback`, so the future is fire-and-forget. Any exception inside `_sync` *should* be caught by the try/catch in `_sync`, but if `_sync` throws *before* entering its try (e.g. await initialization), the future is unawaited. Verified: `_syncGeneration++` is the very first line, so any throw before the try is impossible. Acceptable. | — | observed | None. | False alarm; documenting for the primary thread. |
| D1.9 | `git_manager.dart:_runWithLock` (no-lock branch, ~lines 230-235) | When `typedRunWithLock` is `null`, the catch swallows non-network errors via `Logger.logError(...) return null` — the function returns `null`. Callers that expect a `bool?` (e.g. `backgroundDownloadChanges` returning `Future<bool?>`) interpret `null` as "failure" correctly. Callers expecting `Future<List<X>>` use `?? []` fallbacks (e.g. `getBranchNames`, `listRemotes`). However, `getRecommendedAction` does the right thing (returns null on failure). **Concern:** for `getCommitDiff`/`getFileDiff`/`getWorkdirFileDiff`, the inner catch already returns `null` AND the outer no-lock catch will also return `null` on a different exception class. Acceptable but two layers of catch obscure the originating type. | low | observed | Document the null-means-failure contract on these public methods. | Not a bug but a discoverability hazard. |
| D1.10 | `git_manager.dart:premium_manager.dart:updateGitHubSponsorPremium` (lines 35-58) | The method has multiple branches that set `hasGHSponsorPremium=false` and update `hasPremiumNotifier.value`, but the **flow control is broken**: lines 40-43 and 47-50 set the value and update the notifier but do NOT `return`. Line 53 then unconditionally *also* checks `userRes.statusCode != 200` again to throw, which means the false-set branch falls into the `userNodeId.isEmpty` block. Worst case: a `408` status (timeout) on `userRes` falls through to `jsonDecode(userRes.body)` which is `'Error'` — `jsonDecode('Error')` throws `FormatException`. | high | observed | Add `return` after each `setBool(...false)` branch, or restructure. The `408 != 200` test misses the `408` case being valid for `fileRes` only. | The `if (userRes.statusCode != 200 && userRes.statusCode != 408)` immediately followed by `if (userRes.statusCode != 200) return` means 408 falls through to `jsonDecode(userRes.body)['node_id']` and throws. |
| D1.11 | `premium_manager.dart:updateGitHubSponsorPremium` line 60 `if (userRes.statusCode != 200) {throw Exception('Failed to load sponsors.txt: ${fileRes.statusCode}');}` | Bug: condition is on `userRes.statusCode` but message is `${fileRes.statusCode}`. The intent is clearly `fileRes.statusCode != 200`. As written, this is **dead code** — execution can only reach line 60 if `userRes.statusCode == 200` (line 51 returned otherwise). | medium | observed | Change condition to `fileRes.statusCode != 200`. | Copy-paste bug; sponsor premium check silently treats all `fileRes` failures as "not a sponsor". |
| D1.12 | `premium_manager.dart:cullNonPremium` lines 71-74 | Uses `List.generate(n, (i) async {...})` which **creates a list of orphaned futures**. `List.generate` does NOT await. The `manager.clearAll()` calls run concurrently (or never, if the runtime drops them before execution). Then `setInt(repoman_repoIndex, 0)` and `setStringList(...)` proceed before the cleanup completes. | high | observed | Replace with `for (var index = 0; index < repomanReponames.length - 1; index++) { final manager = await SettingsManager().reinit(repoIndex: 1 + index); await manager.clearAll(); }`. | Data loss / settings residue: non-premium users may keep stale settings for "deleted" repos. |
| D1.13 | `repo_manager.dart:getRepoName` (lines 9-15) | If `index >= repomanReponames.length`, it clamps via `index = repomanReponames.length - 1; setInt(...)`. The `setInt` is **not awaited** — fire-and-forget. Then `(await getStringList(...))[index]` is read again. If `repomanReponames` is empty, `length - 1 = -1` and `repomanReponames[-1]` will throw `RangeError`. | high | observed | `if (repomanReponames.isEmpty) { /* recover */ }`; await the `setInt`. The `repoman_repoNames` default is `["main"]` so this would only occur after explicit clearing. | Crash on empty list; pre-clamp logic also doesn't `await`, so under reentrancy the index is stale. |
| D1.14 | `helper.dart:debounce` (lines 86-90) | Maps `debounceTimers` and `_callbacks` are global mutable state keyed by `String`. If a key fires its timer, neither the timer nor the callback entry is removed from the maps — they grow over the lifetime of the app. With one entry per repo this is bounded, but the iOS bookmark debounce uses `"$iosFolderAccessDebounceReference-$path"` keys (helper.dart line ~1100 area) which can grow unboundedly per bookmark path. Combined with `cancelDebounce(index, run: true)` doing `_callbacks[index]!()` (force-unwrap on potentially-null entry — would NPE if called on a stale/never-set key). | medium | observed | After firing, remove `debounceTimers[key]` and `_callbacks[key]`; null-check in `cancelDebounce`. | Memory leak (bounded by unique keys ever seen) + null-deref hazard in `cancelDebounce`. |
| D1.15 | `helper.dart:waitFor` (lines ~530-540) | `if (result == null) return null;` — function exits the polling loop **on the first null**. This appears intentional ("null means done") but is the opposite of typical `waitFor` semantics. Used by `GitManager.isLocked(waitForUnlock=true)` where `null` means "not locked" — so `null → done` is correct here. **However**: the inner `try { ... } catch (_) {}` swallows all exceptions and continues polling for `maxWaitSeconds`, then returns the final unhandled exception's `await fn()` result on line 540. If the operation has been throwing for 30s, the post-loop call may also throw, and the throw is not caught — propagates to caller. | low | observed | Document the "null=success" inversion; consider returning the last exception. | Subtle semantics. |
| D1.16 | `helper.dart:handleIfNetworkError` (lines ~190-208) | Logic is correct but: the `e.toString()` fallback for non-`AnyhowException` errors may match the `_networkStallPatterns` accidentally if a Dart-side `TimeoutException` with message "timed out" reaches it — that would treat any timeout as a network stall. Since `httpGet/Post/etc` use `onTimeout` to return a 408 Response (not throw), this is unlikely to leak in. But the patterns include "timed out" which is broad. | low | observed | Tighten patterns; consider matching only `AnyhowException` messages. | Possible false-positive retries. |
| D1.17 | `helper.dart:hasNetworkConnection` (line ~178) | `(await Connectivity().checkConnectivity())[0] != ConnectivityResult.none` — indexes `[0]` on a `List<ConnectivityResult>` without a length check. If `connectivity_plus` returns an empty list (documented possibility on platform errors), this throws `RangeError`. | medium | observed | `final results = await Connectivity().checkConnectivity(); return results.isNotEmpty && !results.every((r) => r == ConnectivityResult.none);`. | Crashes the network-error path under platform glitches, which is the *worst* time to crash. |
| D1.18 | `logger.dart:dismissError(BuildContext? context)` line 144 | When `context` is null, the function still pops a navigator using `ErrorOccurredDialog.errorDialogKey.currentContext` if present (line 158), then `if (context == null) return;` skips showing the dialog. Subtle: on null context, an existing dialog gets popped but nothing else happens — acceptable, but `null` does NOT mean "all" or "none"; it means "background dismissal that pops any existing dialog, no new dialog". This matches the call sites (`Logger.dismissError(null)` from `_sync` after success). Document. | low | observed | Add a doc comment. | Non-obvious null semantics. |
| D1.19 | `logger.dart:logError` line 137 | `e("${type.name}: ${"${stackTrace.toString()}\nError: ${error.toString()}"}");` — interpolates stack trace **before** the error, and uses nested string interpolation. Functionally correct but the order is reversed from convention. | low | observed | Use `e("${type.name}: ${error.toString()}\n${stackTrace.toString()}");`. | Cosmetic. |
| D1.20 | `logger.dart:reportIssue` line 173 `await repoManager.setStringNullable(StorageKey.repoman_reportIssueToken, result?.$3 ?? null)` | The `?? null` is redundant; `result?.$3` already evaluates to nullable. Harmless. | low | observed | Drop `?? null`. | Cosmetic. |
| D1.21 | `settings_manager.dart:getGitDirPath` (lines ~110-130) | Returns `path.isEmpty == true ? null : (bookmarkPath, path)`. The `path.isEmpty == true` is a boolean expression that evaluates `path.isEmpty` (returns `bool`) then compares to `true` — works but is a code-smell that may indicate the author was unsure of nullability. Note this `path` is the lambda parameter shadowing the `path` import alias. | low | observed | Rename the parameter to avoid shadowing `path` alias. | Confusing shadowing of import alias. |
| D1.22 | `settings_manager.dart:getGitCommitSigningCredentials` lines ~175-180 | If `key == null` returns `null`. If `key.isEmpty` calls `getGitSshAuthCredentials()` (which returns `(String, String)`, never null). If `key` is non-empty, returns `(passphrase ?? "", key)`. Correct. | — | observed | None. | False alarm; documented. |
| D1.23 | `storage.dart:_get` lines ~125-150 | `if (key.hasDefault && !defaulting) throw StateError(...)` — but `getStringList` and other typed accessors **never set `defaulting=true` except when invoked with that arg**. The static dispatch at the top of `_get` checks `hasDefault && !defaulting`. Many call sites use `getStringNullable(key)` without passing `defaulting=true`. For keys with `hasDefault=true` (e.g. `setman_clientModeEnabled`, `setman_authorName`, `setman_authorEmail`, `setman_postFooter`, etc.), invoking the typed getter without `defaulting=true` throws `StateError`. The wrapper `_getOrDefault` correctly passes `getFn(key, true)` — but any direct `setman.getStringNullable(StorageKey.setman_authorName)` call from another file would explode. | high | observed | Audit call sites of `getBool/getStringNullable/etc` against `hasDefault=true` keys. Or: invert the contract so `hasDefault` is the default-applying mode. | The contract is fragile and footgun-prone; `helper.dart` does call `uiSettingsManager.getStringNullable(StorageKey.setman_gitProvider)` (no `hasDefault`) — those work — but any future caller against a `hasDefault` key throws. |
| D1.24 | `storage.dart:_set<List<String>>` line ~175 | `value == null ? "" : (value as List<String>).join(",")`. **Element values containing `,` are silently corrupted** on roundtrip. `repoman_repoNames` is a `List<String>` of user-provided container names — names can legally include commas. Roundtripping splits incorrectly. | high | observed | Use a proper encoding (URL-encode, JSON, or pick a separator that's banned from container names and validated at input). | Repo-list corruption if a user enters a comma in a container name. |
| D1.25 | `mmap2_flutter` reference (`arch-CF8`) | Grepped target files for `mmap`. **No `mmap` references found** in any of: gitsync_service, manager/{git_manager, storage, settings_manager, repo_manager, premium_manager}, helper, logger, accessibility_service_helper. Use sites must be elsewhere (likely in UI files like `code_editor.dart`/`file_explorer.dart`/`diff_view.dart`), out of scope for this scan. | — | observed | Defer `arch-CF8` follow-up to a UI-layer scan. | Documented for primary thread. |
| D1.26 | `helper.dart:_callbacks[index]!()` in `cancelDebounce` line 96 | Force-unwrap on a possibly-null map lookup. If a caller does `cancelDebounce("foo", run: true)` for a key that never had a debounce scheduled, NPE. | medium | observed | `_callbacks[index]?.call();`. | Crash hazard under user-driven UI flows. |

## Pass 2 — Error Handling and Resilience

| # | Location | Defect | Severity | Evidence Level | Action | Notes |
|---|---|---|---|---|---|---|
| D2.1 | `gitsync_service.dart:_sync` finally block (lines ~298-308) | `if (isScheduled) { ... isScheduled = false; debouncedSync(repomanRepoindex); }` — the `debouncedSync` future is **not awaited**. If the caller of `_sync` `await`s it (e.g. the `debouncedSync` Timer callback), the recursive resync becomes orphaned. The service-isolate `Timer` may tear down before the rescheduled sync even starts. | high | observed | `await debouncedSync(repomanRepoindex);` | Sync stuck / silently no-ops. Specifically: a "scheduled mid-flight" sync may never actually execute on iOS where the widget-callback isolate dies promptly. |
| D2.2 | `gitsync_service.dart:_sync` retry interaction | The retry path: `handleIfNetworkError` schedules a workmanager retry (`scheduleNetworkRetryOp`) when the original sync fails. The `finally` block then sets `isSyncing=false`. The retry fires after ≥30s via workmanager — by then, `isSyncing` is false (good) and `_syncGeneration` has incremented. But the retry calls `_sync` *again*, which increments `_syncGeneration` *again*. Since these happen in sequence, no race. **However**: if the retry fires while a fresh user-initiated sync is in progress (`isSyncing=true`), the retry hits `debouncedSync`'s `if (isSyncing) { isScheduled = true; return; }` path, scheduling *another* sync after the current one completes, with retryCount lost (never re-passed). | medium | strong inference | Pass `retryCount` through `debouncedSync` (it accepts it but the rescheduled call from `_sync` finally block doesn't propagate it). | Retry exponential backoff escapes; effectively retryCount resets to 0. |
| D2.3 | `gitsync_service.dart:_sync` line 247 (`_displaySyncMessage(null, "Credentials not found")` and similar) | Not awaited — `_displaySyncMessage` is `Future<void>` but called as statement expressions. Non-fatal but the toast/notification may race with the subsequent `return;` — on iOS where the function returns and the isolate tears down, the notification may never display. Repeated at lines 249, 251, 270 (5 sites). | medium | observed | Add `await`. | Lost user feedback in the most error-prone paths (auth/conflict). |
| D2.4 | `gitsync_service.dart:_updateForceSyncWidget` line 142 catch | `print('ForceSyncWidget update failed: $e');` — uses `print` not `Logger.logError`, so widget failures don't appear in the report-bug log. Comment says "logged for diagnosis" but logs go nowhere persistent. | medium | observed | Use `Logger.gmLog` or `Logger.logError(causeError: false, ...)`. | Observability gap on platform integration failures. |
| D2.5 | `gitsync_service.dart:merge` (line ~301) | `pushResult = await GitManager.backgroundUploadChanges(...)` — if this throws a non-network error, the `catch (e)` does `if (await handleIfNetworkError(...)) { invoke(MERGE_COMPLETE); return; } rethrow;`. The rethrow does **not** invoke `MERGE_COMPLETE`, leaving the UI's merge-conflict dialog hanging on a never-resolving future. | high | observed | Wrap the rethrow path with `serviceInstance?.invoke(MERGE_COMPLETE);` before `rethrow;`, or use try/finally. | UI deadlock on merge non-network failures; user must force-close. |
| D2.6 | `gitsync_service.dart:merge` line 327 `if (!await settingsManager.getClientModeEnabled()) { debouncedSync(repomanRepoindex, true); }` | Fire-and-forget `debouncedSync`. If it throws synchronously (it shouldn't, but `SettingsManager().reinit` does I/O), the throw is silently swallowed by the unhandled-future runtime. | medium | observed | `await debouncedSync(...)`. | Lost sync trigger. |
| D2.7 | `git_manager.dart:_runWithLock` retry loop (lines ~150-170) | After auto-fix corruption retry, if the retry *also* hits a non-network error, `Logger.logError` is called but the function still returns `null` — the original error and the retry error are both logged but the caller can't distinguish "first attempt failed, retry succeeded" from "both failed". Acceptable, but the pendingNetworkError variable is only set if the retry has a network error, and the original (pre-retry) error is dropped if retry also fails non-networkly. | low | strong inference | Document the policy. | Observability gap. |
| D2.8 | `git_manager.dart:pushChanges` lines 366-374 | `on AnyhowException catch (e, stackTrace) { if (resyncStrings.any(...)) { Logger.logError(...errorContent: changesDuringRebase); } Logger.logError(LogType.PushToRepo, e.message, stackTrace); }` — the `if` branch logs WITH errorContent, then the unconditional second `logError` runs **even on the resync path**, double-logging and double-toasting the same error to the user (because each `logError` writes to `repoman_erroring`). | medium | observed | Add `return;` after the resync-string `Logger.logError`, or use `else`. | Double notifications. |
| D2.9 | `git_manager.dart:backgroundUploadChanges` mergeConflictCallback (lines 695-703) | The callback `await repoManager.setInt(repoman_repoIndex, repomanRepoindex)` **mutates the global current-repo index** as a side effect of a background upload's merge-conflict path. If the user is viewing a different repo in the UI, this silently swaps their context. `sendMergeConflictNotification()` is also fire-and-forget. | high | strong inference | Don't mutate `repoman_repoIndex` from background; route the conflict notification with the repoIndex as data instead. | Cross-repo context swap; serious UX bug. |
| D2.10 | `helper.dart:_performRemoteRepoCreation` line 405-408 | `try { ... } catch (_) { /* Non-fatal — repo is still usable, just without the initial branch setup */ }` — fully empty catch swallowing **all** errors including bugs. The comment justifies it but no error is logged for diagnosis. | medium | observed | At minimum `Logger.gmLog(type: ..., "initial commit/push failed: $e")`. | Silent failure of repo init. |
| D2.11 | `helper.dart:viewOrEditFile` line ~1010 `print(e);` | Caught exception printed via `print` not logger. Then logic falls through to image-extension test. Acceptable for the file-not-text hot-path (FileSystemException on binary read), but any other read error (permissions, etc.) is also silently suppressed. | low | observed | Distinguish FileSystemException from other errors. | Observability. |
| D2.12 | `helper.dart:openLogViewer` line 113-121 | `if (logsDir.existsSync())` then `addAll(...listSync()...)`. The `listSync()` can throw on platform errors (deleted mid-call). No catch. | low | observed | Wrap in try/catch. | Crash hazard in log viewer. |
| D2.13 | `helper.dart:pickDirectory` lines 232-234 | `catch (e, st) { Logger.logError(LogType.SelectDirectory, e, st); } return null;` — error is logged AND returns null; the UI then has no way to distinguish "user cancelled" from "error". | low | observed | Return a richer result (Result/Either) or expose the error. | UX vs. cancel ambiguity. |
| D2.14 | `helper.dart:initialiseStrings` flow / `gitsync_service.dart:initialiseStrings` line 169 | If `serviceInstance.invoke(UPDATE_SERVICE_STRINGS, ...)` is called before the service is up, the new strings vanish; the service uses defaults. No retry/queue. | low | strong inference | Persist the strings via Storage and reload on service start. | Wrong-language sync notifications until the next round-trip. |
| D2.15 | `logger.dart:reportIssue` line 219 catch-less `await http.post(...)` | If the post throws (network down), the exception escapes `reportIssue` and surfaces to the user as a crash. The function is called from a button handler. | medium | observed | Wrap in try/catch with a Fluttertoast for offline. | Crash on flaky network. |
| D2.16 | `logger.dart:dismissError` line 159-161 | `Navigator.of(...).canPop() ? Navigator.pop(...) : null;` — the `: null` makes the ternary expression valid but is functionally a no-op. Cosmetic. | low | observed | Use `if (Navigator.of(...).canPop()) Navigator.pop(...);`. | Style. |
| D2.17 | `accessibility_service_helper.dart:isAccessibilityServiceEnabled` line 60 | `await _channel.invokeMethod('isAccessibilityServiceEnabled') ?? false` — no `try`. If the platform method throws (unsupported platform, channel error), exception propagates. | low | observed | Wrap in try/catch returning `false`. | Cross-platform robustness. |
| D2.18 | `accessibility_service_helper.dart:getApplicationLabel` line 75 | `await _channel.invokeMethod('getApplicationLabel', packageName)` — no error handling, return type is `Future<String>` (non-nullable). If the platform returns null, the `await` will throw `TypeError`. | medium | observed | Either return `Future<String?>` or wrap with default. | Crash on uninstalled-app lookups. |
| D2.19 | `gitsync_service.dart:_finishWidget` Timer (Android) | `_widgetRevertTimer = Timer(const Duration(seconds: 2), () { if (_syncGeneration == gen && !isSyncing) { _updateForceSyncWidget('idle'); }});` — the inner `_updateForceSyncWidget` is `Future<void>`, called from a non-async closure → fire-and-forget, exception unobserved. | medium | observed | Use `() async { if (...) await _updateForceSyncWidget('idle'); }`. | Silent widget-update failure. |
| D2.20 | `repo_manager.dart:setOnboardingStep` line 21 | `if (await getInt(...) == -1) return;` — silently no-ops. If the caller relies on the side effect, they have no way to know it was skipped. (Comment-worthy; -1 = "complete" sentinel.) | low | observed | Document the sentinel. | Discoverability. |
| D2.21 | `git_manager.dart:getRecentCommits` / `getMoreRecentCommits` | Both catch all exceptions and return `[]`. Caller cannot distinguish "no commits yet" from "fetch failed". | low | observed | Return nullable. | Observability. |

## Items routed to defect-scan-semantic

| Suggested ID | Description | Why semantic | Pass |
|---|---|---|---|
| `S-D-1` | `SettingsManager.keyNamespace` is a static field mutated by `reinit()`; concurrent calls (e.g. from accessibility loop + UI sync) corrupt the per-call namespace. | Cross-isolate / cross-call concurrency. | Pass 7 (concurrency). |
| `S-D-2` | `_syncGeneration`/`isSyncing`/`isScheduled` ad-hoc reentrancy gate: synchronous within an isolate but the field is read/written across two isolates (UI + service) per the architecture map. | Cross-isolate concurrency, not single-isolate logic. | Pass 7. |
| `S-D-3` | `accessibilityEvent` may be re-entrant (Android `MethodChannel` callbacks can fire concurrently) and shares static `lastOpenPackageName` / `lastOpenPackageNameExcludingInputs`. | Concurrency. | Pass 7. |
| `S-D-4` | `merge()` client-mode path commits but never pushes — verify intent. | Behavioral correctness, requires understanding client-mode contract. | Pass 4 (contract). |
| `S-D-5` | `backgroundUploadChanges.mergeConflictCallback` mutates global `repoman_repoIndex` — cross-context state mutation. | Behavioral. | Pass 4 / Pass 7. |

## Stats

- Pass 1 final severity tally: **high 5** (D1.10, D1.12, D1.13, D1.23, D1.24), **medium 7** (D1.1, D1.2, D1.5, D1.11, D1.14, D1.17, D1.26), **low 12**, **routed/info 2** (D1.7, D1.25), **non-issue 2** (D1.8, D1.22).
- Pass 2 total: 21 findings. Severity: **high 3** (D2.1, D2.5, D2.9), **medium 9** (D2.2, D2.3, D2.4, D2.6, D2.8, D2.10, D2.15, D2.18, D2.19), **low 9** (D2.7, D2.11, D2.12, D2.13, D2.14, D2.16, D2.17, D2.20, D2.21).
- mmap2 references in scanned files: **none** (D1.25). `arch-CF8` follow-up routes to UI-layer scan, out of scope here.
